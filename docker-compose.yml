version: "3.9"
services:
  nostr-claw:
    build: .
    container_name: nostr-claw
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/data
    restart: unless-stopped
