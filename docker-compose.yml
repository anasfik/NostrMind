version: "3.9"
services:
  nostr-claw:
    build: .
    container_name: nostr-claw
    volumes:
      - ./data:/data
      - ./nostr-claw.config.json:/app/nostr-claw.config.json:ro
    restart: unless-stopped
